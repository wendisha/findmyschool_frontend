//when @@INIT gets fired, it returns the default state of all of our reducers
//Since its a default export, we can call it whatever we want (we'll call it users)
export default (state = [], action) => []